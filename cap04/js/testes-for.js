const objetos = ['bola', 'mesa', 'cadeira', 'prato'];

objetos.forEach((objeto, indice) => {
    console.log(`O objeto ${objeto} está localizado no índice ${indice}`);
})


console.log(objetos.slice(1, 2));